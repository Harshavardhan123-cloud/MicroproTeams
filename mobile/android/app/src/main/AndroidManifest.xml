<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Essential Network Permissions -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />

    <!-- Audio & Video Calling Permissions -->
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.MODIFY_AUDIO_SETTINGS" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <!--
      Bluetooth headset (SCO) audio routing during calls.
      BLUETOOTH_CONNECT covers Android 12+/API 31. minSdkVersion is 24, so API 24-30
      devices still need the legacy BLUETOOTH permission for AudioManager SCO routing;
      maxSdkVersion="30" keeps it off the runtime-permission surface on newer OSes.
    -->
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" />
    <uses-permission android:name="android.permission.BLUETOOTH" android:maxSdkVersion="30" />

    <!-- Notifications (required to post any notification on Android 13+/API 33) -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- Background Call Services -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_CAMERA" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MICROPHONE" />

    <!--
      Screen sharing (mediaDevices.getDisplayMedia()).
      react-native-webrtc runs MediaProjection from a foreground service; targetSdkVersion
      is 34, and API 34 requires the granular FGS-type permission that matches the
      service's android:foregroundServiceType or startForeground() throws
      SecurityException the moment the user starts sharing.
    -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PROJECTION" />

    <!-- Optional Hardware Features -->
    <uses-feature android:name="android.hardware.camera" android:required="false" />
    <uses-feature android:name="android.hardware.camera.autofocus" android:required="false" />
    <uses-feature android:name="android.hardware.microphone" android:required="false" />

    <application
      android:name=".MainApplication"
      android:label="@string/app_name"
      android:icon="@mipmap/ic_launcher"
      android:roundIcon="@mipmap/ic_launcher_round"
      android:allowBackup="false"
      android:theme="@style/AppTheme"
      android:usesCleartextTraffic="true"
      android:supportsRtl="true">
      <activity
        android:name=".MainActivity"
        android:label="@string/app_name"
        android:configChanges="keyboard|keyboardHidden|orientation|screenLayout|screenSize|smallestScreenSize|uiMode"
        android:launchMode="singleTask"
        android:windowSoftInputMode="adjustResize"
        android:exported="true">
        <intent-filter>
            <action android:name="android.intent.action.MAIN" />
            <category android:name="android.intent.category.LAUNCHER" />
        </intent-filter>

        <!-- Deep Linking for Meeting Links: /meet/:code -->
        <intent-filter>
            <action android:name="android.intent.action.VIEW" />
            <category android:name="android.intent.category.DEFAULT" />
            <category android:name="android.intent.category.BROWSABLE" />
            <data android:scheme="micropro" android:host="meet" />
        </intent-filter>
        <intent-filter>
            <action android:name="android.intent.action.VIEW" />
            <category android:name="android.intent.category.DEFAULT" />
            <category android:name="android.intent.category.BROWSABLE" />
            <data android:scheme="https" android:host="violin-providers-entries-content.trycloudflare.com" android:pathPrefix="/meet/" />
        </intent-filter>
      </activity>

      <!--
        Screen-share foreground service. The class ships inside the react-native-webrtc
        AAR (com.oney.WebRTCModule.MediaProjectionService) and its library manifest already
        declares it with android:foregroundServiceType="mediaProjection"; it is re-declared
        here only to make the app's screen-share surface explicit and to pin
        android:exported="false". Values must stay identical to the library's or the
        manifest merger will fail.

        NOTE: the service is inert until MainApplication.onCreate() sets
        WebRTCModuleOptions.getInstance().enableMediaProjectionService = true.
      -->
      <service
        android:name="com.oney.WebRTCModule.MediaProjectionService"
        android:foregroundServiceType="mediaProjection"
        android:exported="false" />
    </application>
</manifest>
